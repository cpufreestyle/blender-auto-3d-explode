# 架构治理迭代计划（Architecture Health Roadmap）

> 目标：降低 `server.js` / `main.js` 单体复杂度，消除重复，统一配置与发布流程，提升可维护性。
> 状态：持续更新。每次合并后回填「进度」。

## 现状（已核实）
- `server.js` 当前实测 391 行（2026-09-25，main 上为 1828 行）：经 NE1 模块化、NE4 模型单一来源、L1 结构化日志/中间件、L2 试点、OBJ 支持之后，L5 又按「能力」把服务端拆成 10 个 ES module（见 L5 行，每块都带 DI 接缝、单测与变异验证）。剩余 391 行只做启动引导（代理探测 → Blender 探测 → 目录创建 → 定时清理）+ 目录/常量 + 各工厂接线 + 路由分发 + 静态文件服务 + 进程级异常守卫。
- 图片转3D 三厂商（Meshy/Tripo/Hyper3D）公共逻辑已抽为 `src/providers/image-to-3d.js`（`runMeshyImageTo3D` 等纯函数，返回 `{glbBuffer, manifest}`，N3+NE1 完成）。
- 请求体解析已统一为 `src/body.js` 的 `readBody(req,{maxSize})`：按 Content-Type 分发 JSON/multipart，复用 `src/server-utils.js` 的 `parseMultipartBuffer` 提取文件（NE3 完成，原三套解析器 `parseMultipart`/`readJSONBodyMax`/`readJSONBody` 已移除）。
- 模型清单单一来源：`src/provider-models.js` 供 server 与 `ai-config.html` 共享（NE4 完成）。
- 上传格式支持：`.glb / .gltf / .stl / .obj`（前端 `main.js` `handleFile` 还接受 `.urdf`，由前端 XML 解析、不走 Blender 拆解）。上传大小上限 `MAX_FILE_SIZE = 150MB`（`src/server-utils.js`，前端 `main.js` 硬校验同步）。
- `main.js` 当前实测 688 行（2026-09-25）：L2 前端模块化两轮完成（见下方 L2 行），3D 拆解控制、几何拆分、相机适配、样式切换、自定义模型加载/面板/收尾、模型归一化、拆卸释放、装配顺序对接、Quest 3 默认模型构建、GLTF 惰性加载、状态条/加载覆盖层、主题与样式切换、生成库加载、键盘快捷键、步骤描述淡入、场景初始化均已迁至 `src/` 下 ES module；剩余为渲染循环（`animate`）、窗口 resize、事件监听引导（`setupUpload` / `setupAIPaint` / `createARPreview` / `setStepUIHook`）、共享状态声明与各工厂实例的 getState/setState 桥接等应用外壳代码。
- 版本号曾脱节：`package.json` `2.0.0` vs release `v3.2.3`（N2 已对齐 `84c9cbd`）。
- release 流程曾手动 `gh` 导致误标 Latest（已纠正为 v3.2.3；后续统一走 `scripts/create_release.sh`）。

## Now — Sprint 1（已承诺）

| ID | 任务 | MoSCoW | 状态 |
| --- | --- | --- | --- |
| N1 | 统一 release 流程：发布走 `scripts/create_release.sh`（自带 `--latest`+工作区校验），弃用手敲 `gh` | Must | ✅ 已纠正 Latest→v3.2.3；脚本待后续增强 |
| N2 | `package.json` 版本号对齐到 `v3.2.3` 并推送 | Must | ✅ `84c9cbd` |
| N3 | 抽离图片转3D 三厂商公共逻辑（`pollTask` / `requireProviderKey`） | Must | ✅ `4bfd0db`（去重 ~66 行） |

## Next — Sprint 2–3（已规划）

| ID | 任务 | MoSCoW | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| NE1 | `server.js` 模块化拆分：云端图片转3D 厂商函数抽至 `src/providers/image-to-3d.js`（纯函数，返回 `{glbBuffer, manifest}`）；`server.js` 降至 ~1530 行 | Should | N3 | ✅ 已完成 |
| NE2 | 复用 `src/server-utils.js`：移除并行实现，`server.js` 仅引入 `getCORSHeaders`/`cleanupOldTempFiles`/`MAX_FILE_SIZE` | Should | 无 | ✅ 已完成 |
| NE3 | 统一请求体解析：`src/body.js` 的 `readBody(req,{maxSize})` 按 Content-Type 分发 JSON/multipart，复用 `parseMultipartBuffer`，取代三套旧解析器 | Should | NE2 | ✅ 已完成 |
| NE4 | 配置 schema 化 + 模型单一来源：`src/provider-models.js` 抽模型清单，server 与 `ai-config.html` 共享；`handleAIConfigPost` 增加校验 | Should | 无 | ✅ `7c32679` |

## Later — Sprint 4+（方向性）

| ID | 任务 | MoSCoW | 状态 |
| --- | --- | --- | --- |
| L1 | 统一路由/中间件 + 错误响应 + 结构化日志（`wrap(handler)` 收敛 try/catch 与 `console.log`） | Could | ✅ `src/logger.js` + `wrap`/`respondError` 中间件 + 进程级异常守卫；`console.*` 重定向为结构化日志 |
| L2 | `main.js` 前端模块化（按 3D 视图/上传/配置面板/VLM 流程拆 ES module） | Could | ✅ 已完成（2026-09-25）。四个试点：① 几何体拆分纯函数迁入 `src/geometry-split.js`；② 材质/乐高外观系统迁入 `src/lego-materials.js`；③ 「配置面板 + Blender 健康检测」迁入 `src/panels/config-panel.js`；④ 「AI 绘画 / 图片转 3D 面板」迁入 `src/panels/ai-paint-panel.js`。其后再完成 10 个模块（`src/assembly-analysis.js` / `src/model-disposal.js` / `src/model-fit.js` / `src/custom-model-loader.js` / `src/custom-model-panel.js` / `src/custom-model-finalize.js` / `src/quest3-model.js` / `src/camera-fit.js` / `src/model-style.js` / `src/gltf-loader.js`，`autoSplitModel` 并入 `geometry-split.js`）。`main.js` 1759 → 909 行。二轮（同日）再迁 5 处：状态条与模型加载覆盖层 → `src/status-ui.js`（`352adb4`）；主题切换 + 乐高/原生样式切换 → `src/theme-toggle.js` + `src/style-toggle.js`（`4f1554c`）；生成库加载 + 键盘快捷键 → `src/generated-library.js` + `src/keyboard-shortcuts.js`（`97156bc`）；步骤描述淡入 → `src/step-desc.js`（`9409682`）；场景初始化（scene/网格/粒子/camera/renderer/OrbitControls + 低功耗判定）→ `src/scene-setup.js`（`ff63228`，`WebGLRenderer` 经 `createRenderer` 接缝注入以便 Node 测试替身）。`main.js` 1759 → 688 行。原判断「3D 视图 + 上传面板与 scene/camera/renderer 强耦合、只能最后做」已被证伪：按「逐字搬迁 + 状态桥接 + DI 接缝 + 单测 + 变异验证」纪律可安全拆分；剩余未拆的只有事件监听引导 / 桥接块与编排收尾函数等应用外壳职责（渲染循环与窗口 resize 已于 `24fd465` 迁至 `src/render-loop.js`，`main.js` 现 625 行） |
| L3 | 提升测试覆盖：provider 生成/预检函数单测（当前只能 HTTP 端到端） | Could | ✅ `tests/provider-test.mjs`（mock fetch，22 项全绿，无需真实 Key） |
| L4 | 依赖漏洞治理：`npm audit` 跟进（default 分支存在 high 级漏洞，见 GitHub Dependabot 告警） | Must(技术健康) | ✅ lockfile 已修（npm audit fix）；GitHub Dependabot 因缓存滞后仍显 3 high，以 lockfile 为准 |
| L5 | `server.js` 模块化：按能力拆 ES module，只留启动引导与路由分发 | Could | ✅ 已完成（2026-09-25）。`server.js` 1828 → 391 行（-1437，-79%），新拆 10 块：AI 配置存储与配置路由 → `src/ai-config.js`（`d546a7f`，337 行，60 断言）；静态文件服务 → `src/static-server.js`（`e55f8be`，144 行，96 断言）；Blender MCP addon 客户端（TCP 行分隔 JSON）→ `src/blender-mcp-client.js`（`154542a`，87 行，27 断言）；AI 调用层四层调度 → `src/ai-call.js`（`3f59f09`，118 行，55 断言）；零配置代理自动探测 → `src/proxy-detect.js`（`b5f3cbd`，56 行，26 断言）；全自动闭环 → `src/closed-loop.js`（`ee8d1c4`，220 行，114 断言）；响应工具 → `src/response-utils.js`（`627dd61`，240 行，108 断言）；Blender 后台任务（单飞串行队列 + 两个 runner）→ `src/blender-runner.js`（`de09d9e`，112 行，42 断言）；AI 生成类路由 → `src/routes-generate.js`（`4a0c403`，203 行，120 断言）；Blender 类路由 → `src/routes-blender.js`（`e9c38f1`，353 行，136 断言）。统一接缝模式：`create*({...})` 工厂带回调与副作用入口，纯函数/内置模块直接 import，目录类参数（`rootDir`/`uploadDir`）显式注入（模块在 `src/` 下，写自身 `__dirname` 会差一级），会触网的依赖（`callAI`/`generateImageTo3D`/`runHyper3DTextTo3D`）也注入以便单测不连云端 |

## 依赖与风险
- **N3 → NE1**：先抽公共逻辑，再拆模块，避免拆出多份重复。
- **NE3 → NE2**：统一解析前先确认 `server-utils` 接口复用方式。
- **发布风险**：手动 `gh` 因上下文过期会把旧 release 误标 Latest；统一到脚本消除。
- **依赖审计限制**：本地 `npm audit` 经 npmmirror 镜像无法访问 security 接口；需用官方源或查看 GitHub Dependabot 告警。
- 容量：70% 功能 / 20% 技术健康 / 10% 缓冲；N3+NE2 属技术健康投入。

## 进度日志
- 2026-07-24：N2/N3 完成并推送；启动 NE1-NE4 + 依赖治理（用户指令「1 2 3」）。
- 2026-07-24：依赖 3 项 high 漏洞修复（npm audit fix，传递 devDeps）；NE1（providers 模块抽出 `src/providers/image-to-3d.js`）+ NE2 + NE3（统一请求体解析为 `src/body.js` 的 `readBody`）完成，server.js 约 1854→1530 行（NE1 完成时；含后续 L1/L2/OBJ/150MB 等增量，当前约 1600 行），冒烟测试通过（/api/health、/api/ai-config 均 200）。NE4（模型清单单一来源）随后完成（提交 `7c32679`）。
- 2026-07-24：L3 落地 — 新增 `tests/provider-test.mjs`，mock 全局 fetch 测试 `src/providers/image-to-3d.js` 的 Meshy/Tripo/Hyper3D 三个纯函数（成功路径返回 `{glbBuffer, manifest}`、缺失 Key 抛 `status=400`、失败状态上抛），22 项全绿且无真实网络请求；`package.json` 的 `test` 脚本已串联 `unit-test` + `provider-test`。
- 2026-07-24：L1 落地 — 新增 `src/logger.js` 结构化日志器（时间戳+级别+meta，`LOG_LEVEL` 过滤，写 stderr）；`server.js` 将 `console.*` 重定向到该 logger 统一全部日志；新增 `wrap(handler)` 中间件 + `respondError(res,err)` 统一错误信封（按 `err.status` 取码，默认 500，防连接挂起），并用 `wrap` 包裹 `createServer` 回调；追加进程级 `uncaughtException`/`unhandledRejection` 守卫。冒烟测试通过（/api/health、/api/ai-config=200，未知路由 404，日志为结构化输出）。
- 2026-07-24：L2 试点 — 抽取 `main.js` 几何体拆分纯函数（extractFacesToGeometry / splitByConnectedComponents / splitByMaterialGroups / splitSpatially / generatePartName）至 `src/geometry-split.js`（ES module，复用同一 three 实例与 `src/utils.js` 的 UnionFind/generatePartName）；`main.js` 删除原 5 个函数体并改为导入，`node --check` 与 `npm test`（22 项）均通过。`main.js` 减约 150 行，行为不变；因共享状态耦合且无法在此环境运行 Three.js 预览，完整前端拆分（视图/上传/配置/VLM 面板）留待最后单独做。
- 2026-07-24：L2 试点② — 抽出 `main.js` 材质/乐高外观系统（`materials` 常量 + `makeLegoMaterial`/`makeLegoGlass`/`legoMaterials`/`nativeToLego`/`brightenToLego`/`getLegoMaterialForMesh`）至 `src/lego-materials.js`；`currentModelStyle` 状态与 `applyModelStyle` 切换逻辑因需改写该状态而保留在 `main.js`。`node --check` 与 `npm test`（22 项）通过，main.js 进一步减负、行为不变。
- 2026-07-24：新增 OBJ 格式支持（上传 + 拆解），提交 `f69d82b`。四处闸门全部打通：`server.js` `/api/split` 格式白名单加入 `.obj`；`blender_split_glb.py` 的 `import_model()` 新增 `.obj` 分支（`bpy.ops.wm.obj_import`，回退 `bpy.ops.import_scene.obj`，均按 Blender 版本探测）；`main.js` `handleFile` 校验数组加入 `obj`；`index.html` 上传提示与 `file-input` accept 加入 `.obj`。端到端验证：上传双立方体 OBJ（972 顶点/768 面）→ Blender 成功导入并拆出 6 个部件 → 返回 GLB（HTTP 200，X-Total-Parts: 6）。
- 2026-07-24：修复 `src/body.js` 的 multipart boundary 大小写敏感回归（L1 重构引入），含于提交 `f69d82b`。原 `readBody` 对 `content-type` 整体 `toLowerCase()`，会破坏大小写敏感的 boundary 值（RFC 2046），导致含大写字母的 boundary（浏览器/脚本生成的 `FormData` 上传常见）提取不到文件、报错「未能从请求中提取文件」。改为仅对「是否 multipart」判断做小写化，boundary 值保持原始大小写。该 bug 同样影响 GLB/GLTF/STL 等所有上传，对所有格式均生效。
- 2026-07-25：上传文件大小上限 100MB → 150MB。提交 `fa1ed7f`（`src/server-utils.js` 的 `MAX_FILE_SIZE` + `index.html` 提示文案）+ 提交 `e6b9cdc`（修正 `main.js` `handleFile` 中遗漏的 `file.size > 100MB` 前端硬校验，文案同步为 150MB）。三处（后端上限 / 前端硬校验 / 提示文案）现已一致。
- 2026-07-26：L2 继续 — 抽取 `main.js` 的「配置面板 + Blender 健康检测」到新模块 `src/panels/config-panel.js`（`fetchConfigAndHighlight` / 配置保存消息监听 / `fetchBlenderHealth` / `updateBlenderUI` / `launchBlender`，仅依赖 DOM+fetch，无 3D 场景耦合）。`main.js` 顶部 `import { fetchConfigAndHighlight }` 并删除原内联实现；`node --check` + `npm test`（22 项）均通过，行为不变。`main.js` 减约 70 行。剩余（3D 视图 / 上传 / VLM·AI 绘画面板）因与 scene/camera/renderer/动画循环强耦合 + 此环境无法跑 Three.js 预览，仍放最后单独做。
- 2026-07-26：L2 继续 — 抽取 `main.js` 的「AI 绘画 / 图片转 3D 面板」到 `src/panels/ai-paint-panel.js`。以依赖注入解耦共享状态：`setupAIPaint({ loadCustomModel, showStatus })`，`BLENDER_SERVER_AI`/`aiPaintGallery` 内移为模块级，`base64ToUtf8` 直引 `../utils.js`、`fetchConfigAndHighlight` 直引同目录 `config-panel.js`。`main.js` 改 `import { setupAIPaint }` + 副作用 `import "./src/panels/config-panel.js"`（确保 Blender 健康检测不再依赖 AI 面板是否启用），底部初始化改为注入依赖调用；`node --check` + `npm test`（22 项）全绿，行为不变。`main.js` 再减约 600 行。L2 四块面板已完成其三（配置 / Blender 健康 / AI 绘画），仅剩 3D 视图 + 上传面板（与 scene/camera/renderer 强耦合）仍未拆。
- 2026-07-26：VLM 部件语义标注实测 + 修复。用 `red_car.glb`（17 部件）经 `blender_split_glb.py --vlm-provider stepfun` 实跑：渲染 17 张部件图正常、环境变量 `VLM_API_KEY` 传递正确、Blender 自带 Python 可直连 `api.stepfun.com`（200）。**发现真问题**：17 图一次性请求推理超 60s 被 `urllib` 超时掐断 → 优雅回退几何名。修复 `call_vlm_vision_multi` 超时 60s→180s（`runBlenderSplit` 整体 600s 余量足够）。另用纯 Python 发 6 图实测 stepfun：24.9s 返回规范 JSON（`[{"idx":1,"name":"红色主体"},...]`），证明多图路径与解析正确（推算 17 图≈70–120s，在 180s 内）。完整「Blender 渲染+标注」长链路在本沙箱跑不完（detached 进程被回收），本机浏览器流程不受影响。
- 2026-07-26：新增「Blender 单飞守卫」（回应「最多弹出一个 Blender」）。`server.js` 增加 `enqueueBlenderJob`（后台拆解/AI 绘画 `--background` 任务串行执行，任意时刻只跑一个，互不重叠）与 `activeBlenderChild`/`killTrackedBlender`（GUI 开窗前先结束上一个由本服务器拉起的窗口，避免堆叠）。保留常驻的 Blender MCP 宿主（监听 9876，不归服务器管）。`node --check` + `npm test`（22 项）全绿，服务器已重启生效。
- 2026-07-27：任务4 收口（`blender_split_glb.py` 的 VLM 参数）：原 `--vlm-provider`/`--vlm-model` 解析与 `VLM_API_KEY` 环境变量读取早已随 `15969bb` 落地；本轮补强「省略 `--vlm-model`」场景——将 `VLM_PROVIDERS` 提升为模块级并加 `default_model`（openai→gpt-4o / kimi→moonshot-v1-8k / stepfun→step-3.7-flash / anthropic→claude-3-5-sonnet-20241022），`semantic_label_parts` 在缺省时回退默认模型并优雅校验 provider，使「`--vlm-provider` + `VLM_API_KEY`」即可独立跑通。`py_compile` 通过，默认模型解析校验通过。
- 2026-09-25：L2 收口 — 前端模块化全部完成，`main.js` 1759 → 909 行（-850）。本迭代在四个试点之后再迁 10 处：装配顺序对接与自定义步骤生成 → `src/assembly-analysis.js`（`8dcf0d7`）；模型拆卸与 GPU 释放 → `src/model-disposal.js`（`6870d57`）；加载后归一化 → `src/model-fit.js`（`194fa5e`）；自定义模型加载主链路 → `src/custom-model-loader.js`（`820fbae`）；自定义模型面板 → `src/custom-model-panel.js`（`eb60496`）；`autoSplitModel` 并入 `src/geometry-split.js`（`1f5090f`）；加载统一收尾 → `src/custom-model-finalize.js`（`4e44535`）；Quest 3 默认模型构建 → `src/quest3-model.js`（`d8e01e9`）；相机自动适配 → `src/camera-fit.js`（`26b09aa`）；样式切换 + GLTF 惰性加载 → `src/model-style.js` + `src/gltf-loader.js`（`b9ce42b`）。每块同一纪律：逐字搬迁 + difflib 归一化等价性证明（均 ratio 1.0）+ 新单测 + 变异验证（全迭代变异全杀）+ 门禁（`npm test` 18 套件 1144 断言全绿、`lint:check` 0 error / 27 warnings 基线、`build` 通过）。共享状态统一为「main.js 持 `let` + 工厂实例 getState/setState 桥接」，「早声明后赋值 + `no-use-before-define`」守护 TDZ 约定。**当前状态**：10 个提交在本地线性分支栈 `refactor/mainjs-*-extract` 上（栈底 `8dcf0d7` 叠在 `main` 之上），本机到 github.com / api.github.com / pypi.org 网络不通（LibreSSL SSL_ERROR_SYSCALL），尚未推送与合并；恢复后按栈序 drain，每个 PR 等 Frontend / Python / Blender 三个 CI job 全绿再 squash 合并。
- 2026-09-25（续）：L2 二轮 — 「应用外壳」继续瘦身，`main.js` 909 → 688 行。本迭代再迁 5 个提交：状态条/loading 覆盖层 → `src/status-ui.js`（`352adb4`，19 断言，变异 11/11）；主题 + 乐高/原生样式切换 → `src/theme-toggle.js` + `src/style-toggle.js`（`4f1554c`，15+18 断言，变异 18/18）；生成库加载 + 键盘快捷键 → `src/generated-library.js` + `src/keyboard-shortcuts.js`（`97156bc`，26+18 断言，变异 21/21，`API_BASE` import 随之移出 main.js）；步骤描述淡入 → `src/step-desc.js`（`9409682`，9 断言，变异 10/10，中途补「animation 赋值序列」断言杀掉「首态不置 none」幸存变异）；场景初始化 → `src/scene-setup.js`（`ff63228`，38 断言，变异 23/23，`new WebGLRenderer` 改 `createRenderer` 接缝注入）。纪律同一：difflib 归一化等价（status-ui/theme/style/step-desc/scene 均 ratio 1.0，inputs 两处桥接）+ 新单测 + 变异全杀 + 门禁（`npm test` 25 套件 1287 断言全绿、`lint:check` 0 error / 27 warnings 基线、`build` 通过）。经验两条：① difflib 基线要把「留守 main.js 的行」（setTimeout/注释）先剔除再比，否则误报 delete；② 中间态行为（如 animation 先置 none）要靠「写入序列」断言钉住，终态断言杀不掉。**当前状态**：16 个提交在本地线性分支栈上（栈底 `8dcf0d7` 起），本机到 github.com / api.github.com / pypi.org 网络持续不通，尚未推送；恢复后按栈序 drain，每个 PR 等 Frontend / Python / Blender 三个 CI job 全绿再 squash 合并。
- 2026-09-25（L5）：`server.js` 模块化 — 按能力拆掉 10 个 ES module，`server.js` **1828 → 391 行（-1437，-79%）**。本迭代 10 个提交，全部同一纪律「difflib 归一化等价 + 新单测 + 变异验证 + 门禁三连 + 真机冒烟」：AI 配置存储与配置路由 → `src/ai-config.js`（`d546a7f`，337 行，60 断言）；静态文件服务 → `src/static-server.js`（`e55f8be`，144 行，96 断言）；Blender MCP addon 客户端 → `src/blender-mcp-client.js`（`154542a`，87 行，27 断言）；AI 调用层四层调度 → `src/ai-call.js`（`3f59f09`，118 行，55 断言）；零配置代理自动探测 → `src/proxy-detect.js`（`b5f3cbd`，56 行，26 断言）；全自动闭环 → `src/closed-loop.js`（`ee8d1c4`，220 行，114 断言）；响应工具 → `src/response-utils.js`（`627dd61`，240 行，108 断言）；Blender 后台任务（单飞串行队列）→ `src/blender-runner.js`（`de09d9e`，112 行，42 断言）；AI 生成类路由 → `src/routes-generate.js`（`4a0c403`，203 行，120 断言，变异 44 个 / 42 杀 / 2 等价存活）；Blender 类路由 → `src/routes-blender.js`（`e9c38f1`，353 行，136 断言，变异 54 个 / 49 杀 / 5 等价存活）。门禁终态：`npm test` 35 套件 2071 断言全绿、`lint:check` 0 error / 27 warnings（与基线一致，两个新测试文件 0 warning）、`build` 通过。**接缝模式固化**：`create*({...})` 工厂统一带名注入「回调 + 副作用入口」，纯函数与内置模块直接 import；目录类参数（`rootDir` / `uploadDir`）必须显式注入——模块位于 `src/` 下，写自身 `__dirname` 会比仓库根少一级；会触网的依赖（`callAI` / `generateImageTo3D` / `runHyper3DTextTo3D`）也注入，否则单测只能连真实云端（`closed-loop` 与 `routes-generate` 都曾因 DI 漏传而静默跑成真实现，只报一句「Meshy 未配置 API Key」了事）。**新经验四条**：① difflib 归一化前先做符号替换表（`UPLOAD_DIR`→`uploadDir`、`__dirname`→`rootDir`、`execFileAsync`→`execFile`），逐条列出差异再人工确认，`routes-blender` 264 个非空行命中 262 行、`routes-generate` 151/151；② 变异锚点遇到「两个函数前两行逐字相同」时必须带上各自的日志文案或后续切片值区分（吃了三个 `SKIP-ANCHOR-MISS` 才想明白）；③ 「等价变异」要能逐条说清为什么杀不掉——`!prompt` 与 `prompt === ""` 在 `.trim()` 之后恒等、`BLENDER_PATH && BLENDER_PATH !== "blender"` 与 `BLENDER_PATH` 在四种取值下三元结果逐一相同、纯日志切片值与 JSON 缩进不影响行为；④ 上限做不起真边界时（`MAX_FILE_SIZE` 150MB）改为钉住「没被人换成更小的上限」，用 24MB 与 25MB+1 夹住 25MB 同理。**当前状态**：本地线性分支栈含 L2 两轮与 L5 共 28 个提交未推送——栈底 `8dcf0d7`（本地 `main` 也在这一点，比 `origin/main` `a49341b` 多 1 个），其上再叠 L5 的 11 个提交（10 个抽取 + 1 个本文档回填 `3e8a06e`）；相对远端 `origin/main` 共 28 个。本机到 github.com / api.github.com / pypi.org 网络不通（curl 回 `http=000`、LibreSSL `SSL_ERROR_SYSCALL`），尚未推送；恢复后按栈序 drain，每个 PR 等 Frontend / Python / Blender 三个 CI job 全绿再 squash 合并。

- 2026-09-25（晚）：L2 之后的大巩固轮 — 模块化收尾 + lint 治理 + 死代码清理 + 单测补强 + 两个静态页单一数据源化，共 29 个提交（`03d87e1`…`55d16cf`）。① 模块化：增强灯光 → `src/lighting.js`（`03d87e1`）；部件步骤序号 → `src/quest3-steps.js`（`66d46d5`）；渲染循环 + 窗口 resize → `src/render-loop.js`（`24fd465`，`window.*`/`requestAnimationFrame`/`document.hidden` 自此退出 main.js）；图片特征提取从「巨型 setup 函数内部」抽纯函数 → `src/panels/image-features.js`（`1f6a6ab`，与「从 main.js 抽块」并列的第二种抽取套路）；GLB 请求公共层 + 提示词图标 → `src/panels/glb-request.js` + `prompt-icon.js`（`9b401a3`）；`src/panels/` 两个 4 空格缩进老文件纳入 eslint 并清掉因此暴露的死代码（`41805e5`）；服务/供应商侧 `readLocalJobResult`（`35989e3`）、`createHyper3DTask`（`5c70968`）、`nearestTemplateIndex`（`ad4098c`）、`readFileAs`/`loader` 注入（`6335f44`）。② lint：10 个游离在 eslint 之外的 src 文件纳入对应 files 块（`9d49e22`），19 条共用规则提至 `baseRules`（`ee88d7c`），告警 48→43（`246da37`，余下 no-console 为有意保留的 warn 基线）。③ 死代码与真 bug：URDF 外部 mesh「需单独上传」提示被抹（`19fd2fe`，行为变更）；删零引用 `partInfo`（`2ad159f`）/`SERVER_PORT`（`8b26eb2`）；删 `part-tooltip` 死标记与死样式、整个孤儿样式表 `upload-enhancement.css`，并建 `tests/dead-markup-test.mjs` 死代码守卫（`c467ab1`/`3393085`）；修 `providers.meshy.model`「取不到元素就静默用默认值」锁死（`e16d0c1`）与同样写入即蒸发的 `providers.hyper3d.mode`（`aa743e3`），删除只扫内联脚本、本地即红的失明脚本 `scripts/check_html_ids.py`。④ 单测补强：`model-loaders`（`6c22d3a`，变异 43 杀 / 9 等价 / 1 盲区）、`explode-geometry`（`53262bd`）、`lego-materials`（`d408a4c`）、`geometry-split` 直接用例（`1aa0f1a`）、`configNeedsHighlight`（`3d48c94`）。⑤ 本轮收尾两手：两个静态页改走共享模块（`65ea155`——`API_BASE` 三处解析合一、配置提醒规则两处合一；顺带修掉一个真 bug：`<meta name="api-base">` 此前只被两个内联脚本读，主应用模块读不到，改标签会让页面与主应用指向不同后端）；`index.html` 静态资源版本键与 `package.json` version 一致性守卫（`55d16cf`）。门禁终态：`npm test` 50 套件 2984 断言 0 失败、`lint:check` 0 error / 43 warnings（基线）、`lint:css:check` 0 error、`build` compiled successfully、`python3 -m unittest` 36 OK；`main.js` 2886 → 625 行，`src/` 55 个模块，测试链 50 个文件。经验五条：① 朴素 substring 扫死代码会被「同 id 子串」冒充消费者骗过（`.btn.upload-btn`），必须 token 收割 + 词边界；② `getElementById(x)?.value || default` 是真 bug 温床，元素缺失即静默锁死；③ 两套覆盖机制互不连通就是 split-brain（`window.APP_CONFIG` 死钩子 vs `meta` 活旋钮）；④ 冒烟前必须清端口占用，僵尸 Chrome/Node 会让人静默测到旧页面；⑤ 静态页 module 化要给 `onclick=` 入口显式挂回 `window`。**当前状态**：本地线性分支栈 `refactor/serverjs-ai-config-extract` 相对 `origin/main`（`a49341b`）共 58 个非 merge 提交未推送（含本文档回填），58 份 PR body（`pr_26`…`pr_83`）已按栈序备齐；本机到 github.com / api.github.com / pypi.org 网络持续不通（`curl` 回 `http=000`、LibreSSL `SSL_ERROR_SYSCALL` rc=35），尚未推送；恢复后按栈序 drain，每个 PR 等 Frontend / Python / Blender 三个 CI job 全绿再 squash 合并。
